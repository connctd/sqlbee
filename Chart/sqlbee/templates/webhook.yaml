apiVersion: admissionregistration.k8s.io/v1beta1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ .Values.webhook.name }}
  labels:
    app: sqlbee
webhooks:
  - name: {{ .Values.service.name }}.{{ .Values.namespace }}.svc
    clientConfig:
      service:
        name: {{ .Values.service.name }}
        namespace: {{ .Values.namespace }}
        path: "/api/v1beta/mutate"
      caBundle: {{ if .Values.webhook.caBundle }}{{ .Values.webhook.caBundle }}{{ else }}{{ .Files.Get "k8s-ca.pem" | b64enc }}{{ end }}
    rules:
      - operations: [ "CREATE" ]
        apiGroups: [""]
        apiVersions: ["v1"]
        resources: ["pods"]
{{ if .Values.webhook.namespaceSelector }}
    namespaceSelector:
{{ toYaml .Values.webhook.namespaceSelector | indent 7 }}
{{ end }}
